<UserControl x:Class="WendigoJaeger.TranslationTool.Controls.TextPreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WendigoJaeger.TranslationTool.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" CanContentScroll="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <local:TextPreviewRenderControl x:Name="textPreviewRender" />
        </ScrollViewer>
        <ComboBox x:Name="comboZoom" Grid.Row="1" SelectedIndex="{Binding ElementName=textPreviewRender, Path=SelectedZoomFactor, Mode=TwoWay}" ItemStringFormat="P" ItemsSource="{Binding ElementName=textPreviewRender, Path=AvailableZoomFactors}" />
    </Grid>
</UserControl>
